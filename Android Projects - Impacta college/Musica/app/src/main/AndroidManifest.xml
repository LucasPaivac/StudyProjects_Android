<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cedroart.lucas.musica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher2"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FaseAActivity" />

        <service
            android:name=".ToastService"
            android:exported="false" />

        <activity android:name=".FaseBActivity" />
        <activity android:name=".FaseCActivity" />
        <activity android:name=".FaseDActivity" />
        <activity android:name=".FaseEActivity" />
        <activity android:name=".ResultadoActivity"></activity>
    </application>

</manifest>